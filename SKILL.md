---
name: ai-thesis-writing
description: Use when writing a graduation thesis with AI assistance — chapter order, plagiarism self-check, AIGC reduction, MD→Word pipeline, consistency audit. Battle-tested pitfalls from a full thesis cycle.
---

# AI 辅助毕业论文写作 Skill

> 从一次完整的毕业论文 AI 制作全流程（选题→初稿→降重→AIGC 清理→排版→提交）中提炼的经验。
> 适用于中国高校本科/自考毕业论文，重点是**计算机类"系统设计与实现"方向**的论文。

## 这个 Skill 解决什么问题

用 AI 写论文最大的风险不是"写不出来"，而是：

1. **查重率过高** — AI 生成的技术介绍章节与网络资料高度雷同
2. **AIGC 检测不过** — 学校开始用 AI 检测工具筛查
3. **内部自我重复** — 绪论/结论/各章小结天然互相重复，查重率被自己拉高
4. **数据打架** — AI 多轮编辑后，不同章节的数字、术语互相矛盾
5. **排版返工** — 格式问题拖到最后一刻才发现

本 Skill 提供一套经过实战验证的完整流程 + 配套检测脚本。

---

## 一、写作顺序策略（最重要的经验）

```
第5章 系统实现（代码现成，最安全）   ← 第一个写
第4章 系统设计（基于自己的架构）
第1章 绪论
第3章 需求分析
第6章 系统测试（跑真实系统获取数据）
第7章 总结与展望
第2章 相关技术（查重风险最高）       ← 最后写
摘要 + 关键词
```

**为什么这样排：**

- **先写实现，后写理论**：从代码/系统出发的论文，实现章节内容现成、查重天然低；技术介绍章节（第2章）是查重重灾区，放最后写，此时你已经完全消化了这些技术，能用自己的话重写
- **测试章必须跑真实系统**：测试数据不能编。跑真实系统记录响应时间、返回结果、截图。编数据在答辩时一问就穿
- **降重前置**：写完高风险章节立即自查重，不要等全文写完再统一查

## 二、项目结构约定

```
my-thesis/
├── PLAN.md              # 唯一权威进度文件（时间线+任务清单+质量指标）
├── CHANGELOG.md         # 每次改动记录（表格：文件|修改内容|依据）
├── chapters/            # 各章 Markdown 源文件
│   ├── ch01-绪论.md
│   ├── ch02-相关技术.md
│   └── ...
├── references/          # 参考文献（GB/T 7714 格式）
├── figures/             # 图片资源
├── scripts/             # 生成/检测脚本
└── output/              # 生成的 Word 成品
```

**单一计划文件原则**：论文项目中只允许**一个**权威进度文件（PLAN.md）。多个计划文件并存必然导致信息不同步。

PLAN.md 必须包含：
1. 总时间线表格 — 各阶段的时间、状态（✅/🔄/⏳）
2. 当前阶段任务清单 — checkbox 格式
3. 质量指标追踪表 — AIGC 检测率、内部重复对数、查重率，每次测完更新
4. 各章字数统计
5. 文件索引

**每次改动后必须双文件更新**：PLAN.md（进度+指标）+ CHANGELOG.md（改了什么）。

## 三、查重降重

### 3.1 内部重复自查（花钱查重前必做）

查重率的主要来源往往是**论文自己跟自己重复**，这个可以免费修。用配套脚本：

```bash
pip3 install jieba scikit-learn
python3 scripts/check_plagiarism.py ~/my-thesis --glob "chapters/*.md"
```

**已知的结构性高重复模式**（几乎所有论文都会出现，属于正常现象但需要控制）：

| 重复对 | 典型相似度 | 改写策略 |
|--------|-----------|---------|
| 绪论"研究内容" ↔ 各章开头 | 60-75% | 绪论用"将要做什么"未来时态 |
| 绪论 ↔ 结论 | 55-70% | 结论用"做到了什么"+量化指标 |
| 结论 ↔ 各章本章小结 | 50-65% | 小结用过程视角 |
| 测试章总结 ↔ 结论测试部分 | 60-65% | 分析段改引用式（"如表6-11所示"） |

**结构性重复 vs 问题性重复**：
- 结构性（可接受）：指标定义模板、前置条件模板、需求↔测试对照——查重系统不太标红这类
- 问题性（必须修）：同一章内重复罗列相同数字、设计章和实现章贴相同代码块

### 3.2 设计章代码块是查重炸弹

设计章(ch04)和实现章(ch05)如果放了相同的代码块，相似度直接 100%。

**解法**：设计章改为"方法签名表格 + 文字说明"，实现章保留完整代码。实测 100% → 0%。

实现章也不能堆全部代码（像 README 不像论文），分层处理：
- **保留完整代码**：核心算法、工作流构建、关键路由逻辑
- **改为文字+方法签名**：基础设施代码（缓存类、监控类、配置文件、工具注册表）
- **判断标准**：如果一段代码的设计思路用 200 字就能说清楚，就不需要贴完整代码

### 3.3 高危句式（必须改写）

| 高危句式 | 改写方向 |
|---------|---------|
| "XX是基于YY架构的ZZ模型" | 改为具体出处+"该类模型通过..." |
| "XX是一种将A与B相结合的技术范式" | 改为提出者+年份+核心思路 |
| "随着我国XX行业的快速发展" | 改为具体市场数据+趋势描述 |
| "本文围绕XX的设计与实现展开研究" | 改为具体做了什么 |

**降重改写技巧**：加出处（泛化描述→"某某等人在某年提出"）、换句式（主动↔被动、长句拆短句）、加细节（"经典算法"→具体机制描述）、换主语。

### 3.4 降重红线（改完必须验证）

> 改写不是机械替换词语。每次改写后必须检查：

1. **逻辑连贯** — 与前后文衔接自然？
2. **术语一致** — 没有引入新术语？（用 `scripts/check_consistency.py` 验证）
3. **数字准确** — 提到的数字与源表格一致？不能凭印象写
4. **交叉引用同步** — 改了 A 章的数字，B/C 章引用同一概念的地方也要同步

**验证范围 = 所有引用同一数字的文件**，不只是正文章节。实测遗漏重灾区：答辩稿、开题报告、摘要、自查清单——这些文件同样引用关键数字，grep 时容易漏。

```bash
# 术语一致性 + 数字一致性一键检查
python3 scripts/check_consistency.py . --glob "*.md" \
  --terms "三级检索,双通道;管理端,后台系统" \
  --numbers "13项,17项;46个,48个"
```

## 四、AIGC 检测与降 AI 率

AI 辅助写的论文需过学校的 AIGC 检测。

```bash
python3 scripts/check_aigc.py ~/my-thesis --glob "chapters/*.md"
```

脚本双模式：
- **模型模式**：`Hello-SimpleAI/chatgpt-detector-roberta-chinese`（需 torch≥2.4，macOS brew 的 python3.11 可能只有 torch 2.2.2，需用 conda 装）
- **规则模式**（自动降级）：连接词密度、模式化开头、学术套话等关键词评分。实测 2.5 万字论文平均 AI 概率 10%，能定位中等风险段落

**常见 AI 痕迹词**（写的时候就要避免）：
"值得注意的是"、"综上所述"、"第一/第二/第三"式列举、"通过...实现了..."、"具有重要意义"、"不仅...而且...同时...此外..."连续堆叠。

**开源降 AI 工具参考**：
- BypassAIGC (github.com/chi111i/BypassAIGC)
- deai-academic-zh (github.com/houlaisan/deai-academic-zh)

## 五、跨章一致性审计（初稿完成后必做）

**核心原则：代码是唯一事实来源，论文描述必须与代码一致，而非反过来。**

实测教训：凭印象把"11个节点"改成"10个"、删掉某功能模块的描述，后来克隆代码仓库核实——代码里确实有 11 个节点和该模块。按代码写的才是对的。**不确定的数据必须去代码里验证，不能凭上一次会话的记忆改。**

检查方法：
```bash
# 克隆实际代码仓库，统计真实数据
grep add_node src/agents/*.py | wc -l   # 节点数
wc -l src/**/*.py                         # 代码行数
ls src/modules/                           # 模块是否存在
```

然后与论文逐项对照：节点数、意图数、路由数、权重/超参数、功能模块存在性、代码行数。

**答辩材料同样要对齐**：答辩 PPT、演讲稿、问答库通常写得早，代码后续迭代不会同步它们。老师对照论文看 PPT，数字打架当场就能发现。

## 六、MD → Word 生成工作流

**MD 是源文件，Word 是生成物**。改 MD 后用脚本重新生成，不要直接在 Word 里改格式。

用 python-docx 脚本生成 .docx（字体/字号/页边距/图表插入）。生成链路：Markdown → Python 脚本 → Word（不经过 HTML）。

**python-docx 已知坑：**
1. **表格标题重复** — 脚本自动生成"表X-Y"但 MD 源文件已有标题，需检测前段
2. **附录表编号归零** — 硬编码计数器，需传全局参数
3. **致谢/参考文献不进目录** — 必须用 `style='Heading 1'` 而非 `add_paragraph()`
4. **目录不显示内容** — python-docx 只能插入 TOC 域代码，需在 Word 中手动"更新域"；可通过 OxmlElement 操作 XML 自动生成目录条目

**解释器陷阱**：项目 `.venv` 可能是 torch/transformers 环境，缺 python-docx。报 `ModuleNotFoundError: docx` 时换系统 python（如 `/usr/local/bin/python3.11`），不要往 venv 里乱装。

**生成前新鲜度检查**：比较章节 MD 的 mtime 与现有 docx 的 mtime。MD 更新→必须重新生成；正式提交前建议无论如何都重新生成一遍。

**图片插入**：MD 中 `![alt](figures/xxx.png)` 语法由生成脚本自动检测插入。生成前做图片覆盖率验证：
```bash
ls figures/*.png                              # 实际有的图
grep -r "!\[" chapters/ | grep -oE '\(figures/[^)]+\)'  # 引用的图
```

## 七、图表要求

自考/本科论文建议至少：
- **6 张图**：技术路线图（绪论必须有，老师初稿检查必查项）、系统架构图、核心流程图、数据库 ER 图、界面截图
- **5 张表**：技术选型表、功能需求表、API 接口表、测试用例表、测试结果表
- 图表编号格式：图3-1、表4-2

**图片生成方法（优先级）**：
1. **mermaid.ink**（在线 API 无需安装）— base64url 编码 Mermaid 定义后请求 API 生成 PNG
2. **AI 生图 API** — 适合架构图/封面类
3. **PIL/Pillow 程序化绘制**（兜底）— 适合流程图/UI mockup，精确控制布局

**图编号跳号检查**：
```bash
grep -oE '图[0-9]+-[0-9]+' chapters/*.md | sort -u  # 确认每章图号连续
```

## 八、参考文献

格式标准：GB/T 7714。
- 英文文献用真实出版信息，中文文献需去知网核实卷期页
- 无正式论文的技术（框架、库）按 `[EB/OL]` 格式引用 GitHub 仓库/官方文档
- arXiv 预印本必须用 `[EB/OL]` 格式并补访问日期
- 作者写到第三位，余者写"，等"或"，et al."

**知网搜索注意**：知网对自动化访问有 IP 级反爬封锁。两条路径：**手动检索**（高级检索→学位论文→关键词组合，结果发给 AI 整理成 GB/T 7714）兜底；**知网 MCP 服务**（如 Cnki-Mcp）让 AI 直接调用检索工具，效率更高但要控制频率防风控。完整操作见 `references/literature-search-workflow.md`。

**常见硬指标**（以学校通知为准）：参考文献≥35篇、外文≥4-5篇、**近三年文献占比≥80%**（这条最容易不达标，需提前统计）、文中呈现国内外研究现状+理论基础+技术路线图。

**文献综述不能只罗列**："XX提出了XX"的罗列式会被认为深度不够。每个方向必须包含：(a) 代表性工作列举 (b) **对比分析**（不同方案优劣）(c) **选型理由**（本系统为何选A不选B）。

**防编造红线**：AI 可能编造不存在的文献或卷期页。中文文献信息必须以知网页面为准，提交前抽查核实；需要批量核实可借助 citation-verification 类技能。

## 九、字数参考（自考本科）

总字数：1.5万~2万字（申请学位建议≥2万）

| 章节 | 占比 |
|------|------|
| 绪论 | 10% |
| 相关技术 | 15% |
| 需求分析 | 12% |
| 系统设计 | 20% |
| 系统实现 | 25% |
| 系统测试 | 15% |
| 总结与展望 | 7% |

## 十、多 AI 模型协作的坑

论文被多个 AI 工具轮流编辑时，后一个模型不知道前一个改了什么，可能"回退"前一个的修改。应对：

- 每次接手前**先读 PLAN.md 和 CHANGELOG.md**
- 对任何"与上次不一致"的内容，**先查代码/文件确认谁对**，不要假设自己上次是对的
- 数据类修改必须有可验证来源（代码统计结果），不能凭记忆
- 改完后更新 PLAN.md + CHANGELOG.md，让下一个模型能看到

## 十一、提交包准备流程（上传学校平台前）

1. 读 PLAN.md + CHANGELOG.md 尾部，确认当前状态
2. 新鲜度检查后重新生成 Word
3. 建专用提交文件夹：`output/初稿提交-YYYYMMDD/`，放入论文 docx + 附件
4. 写一份 `提交说明.md`：上传哪个文件、上传前检查清单（封面/目录页码/图片显示）、论文现状指标、时间窗口
5. 更新 CHANGELOG.md 记录本次生成

**老师发正式通知后的标准动作**：把通知拆成逐条硬指标，**逐条对照成品实测**，输出对照表（要求|实测结果|状态）。不要凭"应该够"下结论。

## 十二、学校要求摄入：把官方文件喂给 AI 生成硬约束规范

学校发的要求通知、格式规范、撰写模板、评分标准、答辩安排到手后，**第一件事不是动笔写论文，而是把它们全部"喂"给 AI，合并生成一份 `school-requirements-spec.md` 硬约束规范**。之后写/改论文、生成 Word、提交前自查全部以它为准——不靠记忆、不靠"应该够"。

完整流程（文件读取技术 + 规范骨架模板 + 使用协议 + 实测坑）见 `references/school-requirements-workflow.md`，核心四步：

1. **归档** — 学校发的所有文件复制到 `school-docs/` 只读归档，原件不动
2. **提取** — pymupdf 读 PDF（图片型 PDF 提图后用视觉模型读）、textutil 读 .doc、python-docx 读 .docx、openpyxl 读 .xlsx
3. **生成 spec** — 按骨架模板合并成单一权威规范：结构顺序、字数硬指标、标题/图表/参考文献格式、评分标准、提交清单、答辩流程。三条硬要求：数字保留学校原文、写成硬约束（必须/不得/≤/≥）、按主题分章便于局部更新
4. **使用** — 写/改论文前必须加载 spec；提交前逐条对照成品实测；学校发新通知 → 先更新 spec → 再动笔

**为什么值得做**：学校要求散落在多个文件里（通知正文+附件模板+评分表+口头补充），漏看一个就返工；AI 生成内容不符合格式要求，定稿后才发现的返工成本极高。一份单一权威规范文件把这些全部收敛掉。

## 十三、完整坑清单（28 条实战记录）

1. **第2章最容易翻车** — 技术概念网上到处都是，必须用自己的话重写
2. **测试数据不能编** — 跑真实系统，记录响应时间、返回结果
3. **参考文献格式** — 英文文献信息可靠，中文文献必须去知网核实
4. **查重要最后做** — 全文定稿+排版后再花钱查，否则浪费钱
5. **选题改动要先权衡** — 先分析利弊再动手，不要先改再撤回
6. **框架名不宜放题目** — 新框架知网文献少，选题用更宽泛的技术方向，框架作为实现工具在正文引用
7. **绪论/结论是查重重灾区** — 绪论用未来时态，结论用成果导向+量化
8. **PyTorch 版本陷阱** — macOS brew python3.11 的 torch 最高 2.2.2，HF 新版 transformers 需要 torch≥2.4/2.6，用 conda 装或降级
9. **查重自查先行** — 花钱查重前先用 jieba+TF-IDF 自查内部重复
10. **设计章代码块是查重炸弹** — 设计章改方法签名表格，实现章保留代码
11. **实现章代码精简** — 核心算法保留完整代码，基础设施改文字+签名
12. **降重时术语漂移** — 引入新术语导致全文不一致，改完必须 grep 验证
13. **降重时数字出错** — 凭记忆写数字，必须回头核实源表格
14. **跨章引用不同步** — 改了一章的数字，其他章引用处要同步
15. **图表插入占位符** — 批量替换前先 grep 定位所有占位符
16. **图片引用与 figures 目录不一致** — 生成后做覆盖率检查
17. **图编号跳号** — 新增图片时检查编号连续性
18. **降重改写后同章内部冲突** — 绪论"研究内容"和"组织结构"天然撞车，两处用不同切入点
19. **摘要关键词分隔符** — 中文关键词用"；"（分号），不是"、"（顿号）
20. **技术介绍超长段落** — 拆分为选型理由+技术细节两段，每段≤300字
21. **文献综述不能只罗列** — 必须有对比分析和选型理由
22. **跨章一致性审计以代码为准** — 克隆代码仓库逐项核实，不凭记忆
23. **多模型编辑漂移** — 接手先读 PLAN/CHANGELOG，不一致先查证
24. **结构性重复 vs 问题性重复** — 前者可接受，后者必须修
25. **收尾轮不要硬找毛病** — 正文达标后做回归验证+口径核对，不是制造新改动
26. **答辩材料对齐以代码为准** — PPT/演讲稿/问答库的数字逐项对照代码
27. **验证范围包括衍生文件** — 答辩稿、开题报告、摘要同样引用关键数字
28. **计划表自身会过时** — 多模型编辑后字数表、文献数、检测时间都可能陈旧，先对齐再干活

## 配套脚本

| 脚本 | 用途 |
|------|------|
| `scripts/check_plagiarism.py` | 内部查重（jieba + TF-IDF + 余弦相似度） |
| `scripts/check_aigc.py` | AIGC 检测（模型+规则双模式） |
| `scripts/check_consistency.py` | 术语/数字跨章一致性检查 |

所有脚本参数化设计，指定论文目录和文件匹配模式即可用，不绑定具体项目。

## 相关开源工具

| 工具 | 用途 |
|------|------|
| thesis-typeset (153lsr/thesis-typeset) | Python Word 一键排版 |
| ucasthesis (mohuangrui/ucasthesis) | LaTeX 中文论文模板 |
| claude-scholar (Galaxy-Dawn/claude-scholar) | 学术研究全流程技能库（40+ 学术技能） |
| Cnki-Mcp (PluginsX/Cnki-Mcp) | 知网文献检索 MCP 服务（第三方） |
| paperdebugger (PaperDebugger/paperdebugger) | 论文 AI 审阅 |
| BypassAIGC (chi111i/BypassAIGC) | AIGC 降重 |
| mermaid.ink | 在线 Mermaid 图生成 |
